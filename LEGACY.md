# 기존 수강생용 버전 (legacy)

이 브랜치는 **기존 수강생 전용 버전**입니다. `hoonpro.vercel.app`에서 서비스합니다.

- 기준 커밋: `18f2a0a` (2026-06-24) — 앱 코드는 이 시점 그대로이며 수정하지 않습니다
- 유료화(구독·결제·쿠폰) 기능이 없고, 로그인은 이메일만으로 이루어집니다
- **Supabase는 유료 버전과 분리된 별도 프로젝트**를 사용합니다
  (테이블: `users` · `api_usage` · `api_calls` · `app_config`)
- `JWT_SECRET`은 유료 버전과 반드시 다른 값을 씁니다

유료 버전(`hoonproai.com`)은 `main` 브랜치입니다. 두 버전은 DB와 배포가 완전히 분리되어 있으므로,
이 브랜치에 유료 버전의 변경사항을 병합하지 마세요.

## 배포 설정

| 항목 | 값 |
|---|---|
| Vercel 프로젝트 | `hoonpro-lecgacy` |
| Root Directory | `./` — **`sourcing`이 아닙니다** |
| Production Branch | `legacy-2026-06` |
| 서비스 주소 | `hoonpro.vercel.app` |
| 관리자 계정 | `ADMIN_EMAIL`과 일치하는 이메일만 관리자 탭이 열립니다 |

서버리스 함수는 **12개**가 나와야 정상입니다. 5개로 나오면 Root Directory가
`sourcing`(별개의 소싱 분석기 앱)으로 잘못 잡힌 것입니다.

이 브랜치는 배포하려면 **커밋을 푸시해야** 합니다. Vercel의 Redeploy는 같은 커밋을
다시 빌드할 뿐이라 브랜치 전환이 반영되지 않습니다.

## 필수 환경변수

없으면 함수가 기동 중 죽고, 화면에는 "네트워크 오류가 발생했습니다"로만 보입니다
(500 HTML을 프론트가 JSON으로 파싱하지 못해 catch로 떨어지기 때문).

- `SUPABASE_URL` · `SUPABASE_SERVICE_KEY` — 레거시 전용 Supabase
- `JWT_SECRET` — 유료 버전과 다른 값
- `ADMIN_EMAIL` — 소문자, 앞뒤 공백 없이

기능별로 `OPENAI_API_KEY`, `GEMINI_API_KEY`, `NAVER_*`, `COUPANG_*`가 추가로 필요합니다.

환경변수는 **배포를 만드는 시점에 함수로 굳습니다.** 이미 떠 있는 배포는 나중에
변수를 넣어도 그 값을 영원히 보지 못합니다. 변수를 추가·수정했으면 **그 뒤에**
새 배포(푸시 또는 Redeploy)를 만들어야 반영됩니다. 순서가 반대면 아무 일도
일어나지 않습니다.

## 증상: "OPENAIAPIKEY가 설정되지 않았습니다"

이미지 생성·이미지 검수·카피 생성에서 뜹니다. 문구의 `OPENAIAPIKEY`는 밑줄이
빠진 게 아니라 코드에 그렇게 적힌 것이고, 변수 이름은 `OPENAIAPIKEY`와
`OPENAI_API_KEY` 둘 다 인식합니다 (`api/generate-image.ts` `apiKey` 계산부).

원인은 **키가 없는 것 하나뿐**입니다. 키 값이 틀렸거나 만료됐을 때는 이 문구가
아니라 OpenAI의 응답 메시지와 함께 **502**가 옵니다. 그래서 상태 코드로 구분이
됩니다.

| 상태 | 뜻 |
|---|---|
| 500 + "…설정되지 않았습니다" | 함수가 변수를 못 봄 — 등록 위치나 배포 순서 문제 |
| 502 | 키는 있고 OpenAI가 거절 — 값·잔액·권한 문제 |
| 429 | 분당 생성 한도 — 앱이 자동 재시도 |

500이면 이 순서로 확인합니다.

1. 변수를 넣은 프로젝트가 **`hoonpro-legacy`**인지 (`hoonproai`는 유료 버전이라
   레거시에 반영되지 않습니다)
2. 그 변수의 **Production 체크박스**가 켜져 있는지
3. 변수를 넣은 **뒤에** 만들어진 배포인지 — Deployments 목록의 배포 생성 시각이
   변수 추가 시각보다 나중이어야 합니다

## 이미지 모델은 45초간 캐시됩니다

관리자 [AI 생성 모델 설정]에서 모델을 바꾸면 `app_config`에 즉시 저장되지만,
함수는 이 값을 45초 캐시합니다 (`api/generate-image.ts`의 `CONFIG_TTL_MS`).
바꾸자마자 눌러보면 이전 모델로 처리되므로, **1분쯤 뒤에** 확인하세요.

모델 계열에 따라 필요한 키가 다릅니다. GPT 계열이면 `OPENAI_API_KEY`,
Gemini 계열이면 `GEMINI_API_KEY`가 있어야 하고, 없으면 **둘 다 똑같이 500**으로
떨어집니다. 문구만 다릅니다.
